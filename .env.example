DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="generate-a-random-secret-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Email (verification + notifications). For local dev, use MailHog or similar on port 1025.
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_SECURE="false"
# SMTP_USER=
# SMTP_PASS=
EMAIL_FROM="LostFound <noreply@lostfound.local>"
